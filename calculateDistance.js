
const calculate = (x, y) => {
    const distance = Math.sqrt(Math.pow(x - 0, 2) + Math.pow(y - 0, 2));
    console.log(distance);
}

const x = prompt("Enter x co-ordinate");
const y = prompt("Enter y co-ordinate");
calculate(x, y);


